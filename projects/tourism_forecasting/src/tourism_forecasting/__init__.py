"""Project A — Maldives monthly tourist-arrival demand forecasting.

Build brief in CLAUDE.md. Models live in `models/`; the end-to-end run is in
`pipeline.py`. Depends on `ave_core` for ingestion, contracts, eval, and export.
"""

__version__ = "0.1.0"
